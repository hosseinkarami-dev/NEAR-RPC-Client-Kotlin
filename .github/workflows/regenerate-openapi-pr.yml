name: Regenerate NEAR RPC Client (auto PR, merge & release)

on:
  workflow_dispatch:
  push:
  schedule:
    - cron: "0 0 * * *" # daily run (every day at midnight)

# Note: removed `push: branches: [ main ]` trigger to avoid self-triggering

permissions:
  contents: write
  pull-requests: write

jobs:
  regenerate-merge-release:
    runs-on: ubuntu-latest

    steps:
      # Avoid infinite loop triggered by GitHub Actions bot
      - name: Exit if triggered by GitHub Actions bot
        if: github.actor == 'github-actions[bot]'
        run: |
          echo "Triggered by GitHub Actions bot; exiting to avoid loop."
          exit 0

      # Checkout repository (full history and tags)
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true

      # Setup JDK
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: 21

      # Grant execute permission for Gradle
      - name: Grant execute permission for Gradlew
        run: chmod +x ./gradlew

      # Run Generator (regenerates client + models)
      - name: Run Generator
        run: ./gradlew :generator:run --args="--openapi-url https://raw.githubusercontent.com/near/nearcore/master/chain/jsonrpc/openapi/openapi.json" --no-daemon

      # Build and run tests
      - name: Build project (and run tests)
        run: ./gradlew build --stacktrace --no-daemon

      # Prepare branch, commit changes, push
      - name: Prepare branch, commit regenerated sources
        id: commit
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail
          git config --local user.email "automation@github.com"
          git config --local user.name "GitHub Actions Bot"

          SHORT_SHA=${GITHUB_SHA:0:8}
          BRANCH="regenerate-openapi-${GITHUB_RUN_NUMBER}-${SHORT_SHA}"
          git checkout -b "$BRANCH"

          git add .
          if git diff --staged --quiet; then
            echo "No changes to commit"
            echo "pr_required=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          git commit -m "fix: regenerate client from OpenAPI"
          git push https://x-access-token:${PAT_TOKEN}@github.com/${GITHUB_REPOSITORY}.git "$BRANCH"

          echo "pr_required=true" >> "$GITHUB_OUTPUT"
          echo "branch=$BRANCH" >> "$GITHUB_OUTPUT"

      # Auto-create PR and merge if changes exist
      - name: Auto-create and merge PR
        if: steps.commit.outputs.pr_required == 'true'
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.PAT_TOKEN }}
          script: |
            const branch = '${{ steps.commit.outputs.branch }}';
            const title = `chore: regenerate client from OpenAPI (${branch})`;
            const body = `This PR regenerates the NEAR RPC client and models from the latest OpenAPI spec.\n\nAutomatically merged after generation.`;

            // Create PR
            const pr = await github.rest.pulls.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              head: branch,
              base: "main",
              body
            });

            // Auto-merge PR
            await github.rest.pulls.merge({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: pr.data.number,
              merge_method: "squash"
            });

      # Stop if no changes
      - name: Output when no changes
        if: steps.commit.outputs.pr_required != 'true'
        run: echo "No regenerated changes — nothing to create a PR for."

      # Make sure local main is up-to-date with remote before tagging
      - name: Prepare local main for tagging
        if: steps.commit.outputs.pr_required == 'true'
        run: |
          set -euo pipefail
          # fetch latest main and tags
          git fetch origin main --tags
          # ensure we are on main and match remote
          git checkout main
          git reset --hard origin/main

      # Determine new tag version (only if we actually created a PR & merged)
      - name: Determine new tag version
        if: steps.commit.outputs.pr_required == 'true'
        id: tag
        run: |
          set -euo pipefail
          # fetch tags from remote to be sure we see the latest tags
          git fetch --tags origin

          # find latest tag (or default to v0.0.0)
          LATEST_TAG=$(git tag --sort=-v:refname | head -n1 || echo "v0.0.0")
          if [ -z "$LATEST_TAG" ]; then
            LATEST_TAG="v0.0.0"
          fi

          # split into MAJOR.MINOR.PATCH (robustly)
          TAG_NO_PREFIX="${LATEST_TAG#v}"
          IFS='.' read -r MAJOR MINOR PATCH <<<"${TAG_NO_PREFIX}"
          MAJOR=${MAJOR:-0}
          MINOR=${MINOR:-0}
          PATCH=${PATCH:-0}

          # compute next available tag: increment patch until unique
          while true; do
            CANDIDATE="v${MAJOR}.${MINOR}.$((PATCH+1))"
            if git rev-parse "refs/tags/${CANDIDATE}" >/dev/null 2>&1; then
              PATCH=$((PATCH+1))
              continue
            fi
            # also check remote explicitly (race with other runners)
            if git ls-remote --tags origin | grep -q "refs/tags/${CANDIDATE}$"; then
              PATCH=$((PATCH+1))
              continue
            fi
            NEW_TAG="${CANDIDATE}"
            break
          done

          echo "new_tag=${NEW_TAG}" >> $GITHUB_OUTPUT
          echo "New tag determined: ${NEW_TAG}"

      # Create and push tag
      - name: Create and push tag
        id: create_tag
        if: steps.commit.outputs.pr_required == 'true'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -euo pipefail
          NEW_TAG="${{ steps.tag.outputs.new_tag }}"

          # ensure we have remote tags
          git fetch --tags origin

          # Double-check that no tag already points to HEAD
          if git tag --points-at HEAD | grep -q .; then
            EXISTING_TAG=$(git tag --points-at HEAD | head -n1)
            echo "Tag already exists that points at HEAD (${EXISTING_TAG}). Skipping tag creation."
            echo "tag_created=false" >> $GITHUB_OUTPUT
            echo "existing_tag=${EXISTING_TAG}" >> $GITHUB_OUTPUT
            exit 0
          fi

          # If NEW_TAG exists (race or pre-existing), attempt to find the next free tag
          attempt=0
          while git rev-parse "refs/tags/${NEW_TAG}" >/dev/null 2>&1 || git ls-remote --tags origin | grep -q "refs/tags/${NEW_TAG}$"; do
            attempt=$((attempt+1))
            echo "Computed tag ${NEW_TAG} already exists. Trying next patch (attempt ${attempt})..."
            # bump patch
            base="${NEW_TAG#v}"
            IFS='.' read -r MAJOR MINOR PATCH <<<"$base"
            PATCH=$((PATCH+1))
            NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            # for safety, break if too many attempts
            if [ $attempt -gt 50 ]; then
              echo "Too many attempts to compute a unique tag. Aborting."
              echo "tag_created=false" >> $GITHUB_OUTPUT
              exit 1
            fi
          done

          # Create annotated tag and push
          git tag -a "${NEW_TAG}" -m "Release ${NEW_TAG} (automated)"
          git push "https://x-access-token:${PAT_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" "${NEW_TAG}"

          echo "tag_created=true" >> $GITHUB_OUTPUT
          echo "created_tag=${NEW_TAG}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        if: steps.create_tag.outputs.tag_created == 'true'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.tag.outputs.new_tag }}
          name: "Release ${{ steps.tag.outputs.new_tag }}"
          body: |
            🚀 Automated release generated by workflow.
            This release was created automatically after merging the regenerated client into main.
        env:
          GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}